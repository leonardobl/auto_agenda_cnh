# packages/contracts

Shared schemas/types/constants between `apps/web` and `apps/api` — not implemented yet.

This is a placeholder directory reserved by the monorepo restructure. It will hold contracts that carry no security rule themselves (per `docs/09_Arquitetura_DevOps_Operacao.md` §2), shared once the backend exists and there's real duplication to eliminate between the two apps.
