# repositories

Data access behind explicit contracts — plain SQL via `node:sqlite` (see "Reconciling with the academic spec" in the root `CLAUDE.md` for why not an ORM). Empty for now — no domain queries exist yet. The first domain feature (e.g. auth) adds its own repository here.
