# modules

Domain modules and use-cases (business rules), one subfolder per domain area (e.g. `auth`, `students`). Empty for now — this bootstrap change adds no domain logic. The first domain feature (e.g. auth) creates its own subfolder here.
