# shared

Cross-cutting utilities: error types, clock abstraction, ID generation. Empty for now — this bootstrap change builds its error envelope directly in the error-handling middleware. Add a shared error class/utility here once a second consumer actually needs one, rather than extracting it speculatively.
