# apps/api

Backend (Node.js) — not implemented yet.

This is a placeholder directory reserved by the monorepo restructure. The API will be built as a separate change, once the framework/ORM decisions in `docs/04_Especificacao_BackEnd_API.md` are reconciled with the project's actual choices (see the "Reconciling with the academic spec" section in the root [CLAUDE.md](../../CLAUDE.md)).
