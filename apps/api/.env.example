# Required — server refuses to start if any of these are missing.
NODE_ENV=development
PORT=3333
APP_ORIGIN=http://localhost:5173

# Optional — overrides where the local SQLite database file is created (defaults to data/app.db).
# DB_PATH=data/app.db
